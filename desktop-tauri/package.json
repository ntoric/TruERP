{
  "name": "truerp-desktop-tauri",
  "private": true,
  "version": "1.0.0",
  "description": "TruERP desktop shell (Tauri) — offline billing ERP",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "prepare:frontend": "./scripts/prepare-frontend.sh",
    "prepare:api": "./scripts/prepare-api.sh",
    "prepare:bundle": "./scripts/prepare-bundle.sh",
    "build:macos": "./scripts/build-macos.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
